@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

if(@SPMV_WITH_CUDA@)
    find_dependency(CUDAToolkit)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/gpu_spmvTargets.cmake")

check_required_components(gpu_spmv)
